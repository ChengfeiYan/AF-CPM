# AF-CPM

AF-Constraint Propagation Mapping 

![](AF-CPM.png)

The pair representations generated by each layer of the 48-layer Evoformer stack are extracted and transform into a distance map using  Distogram Head. The distance map is then normalized to represent the probability of the $C_\beta$ atom distance between two predicted residues at each bin. Afterward, the probabilities for the first 32 bins of the distance distribution map are summed, and the distance distribution map is transformed into a contact probability maps  with a 12Å threshold. These contact probability maps  effectively reflect the modeling process of the complex within the model.

## Requirements

Install OpenFold

## Modified OpenFold file structure

```
openfold/
│
├── openfold/
│   │
│   ├── data/         
│   │   ├── data_pipeline.py  # Modification: support custom MSAs and templates inputs   
│   │   └── feature_processing_multimer.py  # Modification: skip MSA pairing
│   │
│   └── model/      
│       ├── embedders.py # Modification: save pair representations from each Evoformer    layer 
│       └── model.py  # Modification: return all pair representations as a list
│
└── requirements.txt  # Modification: save the pair representations list as a .npy file 
│
└── run.py # Executable file: saves pair representations and transform them into contact maps
```

## Usage

1. Replace the following files in the `openfold/openfold/data` directory with the ones provided in the `data` folder:
   - `data_pipeline.py`
   - `feature_processing_multimer.py`

2. Replace the following files in the `openfold/openfold/model` directory with the ones provided in the `model` folder:
   - `evoformer.py`
   - `model.py`

3. Replace the `run_pretrained_openfold.py` file in the `openfold` root directory with the provided version.

4. Place `run.py` in the same directory as `run_pretrained_openfold.py`, then execute `run.py` to save and transform pair representations.

## Input file structure

```
8G4K/
│
├── alignments/
│   │
│   ├── A/         
│   │   ├── A.a3m   
│   │   └── A.cif
│   └── B/         
│       ├── B.a3m   
│       └── B.cif 
│   
└── fasta/
    └── 8G4K.fasta
```

## Example

```
python run.py
```

## output file structure

```
8G4K/
│   
└── results/
    ├── A-B_1_zstack.npy
    │  
    ├── predictiobs/
    │  └── A-B_model_1_multimer_v3_unrelaxed.pdb
    │  
    └── distogram_1/
        ├── layer_1_contact_map.txt   
        ├── layer_2_contact_map.txt
        ......
```

## Visualization

![](D:\科研\AFppisave\展开\hetero\8G4K_temp.png)
